name: Build Force-Install RPM Artifact

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read
  packages: write

jobs:
  build-force-install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push force-install artifact image
        uses: docker/build-push-action@v7
        with:
          context: .github/force-install-builder/
          file: .github/force-install-builder/Containerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ghcr.io/phantomcortex/distinctionos-force-install:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
